services:
  - docker
script:
  - ./bin/build.sh
  - ./bin/push.sh
env:
  global:
    # RELEASE contains the serverless release to build
    # Ref: https://github.com/serverless/serverless/releases
    - RELEASE=1.48.0
